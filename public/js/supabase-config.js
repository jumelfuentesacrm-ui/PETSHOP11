/* ---------------------------------------------------------------------------
   Supabase connection settings  —  EDIT THESE TWO VALUES
   ---------------------------------------------------------------------------
   In your Supabase dashboard:  Project Settings -> API
     * SUPABASE_URL       = "Project URL"
     * SUPABASE_ANON_KEY  = Project API keys -> "anon" / "public"

   The anon key is meant to live in client-side code; the database is
   protected by row-level security policies (see supabase/schema.sql).
--------------------------------------------------------------------------- */
window.SUPABASE_URL = 'https://YOUR-PROJECT.supabase.co';
window.SUPABASE_ANON_KEY = 'YOUR-ANON-PUBLIC-KEY';
